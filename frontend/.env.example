# Server-side FastAPI base URL. Do not use NEXT_PUBLIC_ for backend-only configuration.
UNCLAIMED_API_BASE_URL=http://127.0.0.1:8000

# Supabase is intentionally not configured in the first read-only reviewer-console slice.
# When introduced later, use a publishable client key only in browser-visible configuration.
